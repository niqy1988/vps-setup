# 示例：一个远端一个文件；可含 raw + crypt 两层（脱敏占位，token 等敏感信息请替换）
[sample_raw]
type = webdav
url = https://<server>/dav
vendor = other
user = <user>
pass = <pass>

[sample]
type = crypt
remote = sample_raw:
password = <crypt_password>
password2 = <crypt_password2>
