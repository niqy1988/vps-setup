---
- name: Restart filebrowser
  become: true
  become_user: "{{ podman_username }}"
  ansible.builtin.systemd_service:
    name: "{{ filebrowser_container }}"
    scope: user
    state: restarted
  listen: Restart filebrowser
