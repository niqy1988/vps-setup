---
dependencies:
  - role: user
    username: "{{ ansible_username }}"
    uid: "{{ ansible_uid }}"
    user_groups: []
    ssh_public_key: "{{ ansible_ssh_public_key }}"
    sudoer: true
    comment: ansible remote user
