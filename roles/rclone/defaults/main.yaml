rclone_config_path: /var/local/rclone

rclone_mounts:
  - name: sample
    config: sample.conf
    remote: sample
    dir: shared
    mount_path: /mnt/sample
  - name: sample2
    config: another_sample.conf
    remote: another
